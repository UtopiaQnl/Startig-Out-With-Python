5.31
    Инструкция return <выражение> позволяет вернуть результат "выражения" обратно
    в ту часть программы, откуда была вызвана функция

5.32
    а) Функция с возратом значения
    б) Умножает переданный её агрумент "number" на 2 и возвращает результат
    в) 20

5.33
    Предикат (функция, которая возвращает либо True, либо False)

