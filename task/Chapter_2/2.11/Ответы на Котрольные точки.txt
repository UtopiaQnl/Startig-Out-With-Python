2.40 * Именнованные константы устраняют проблему "Магических значений", т.е. решают сразу несколько проблем:
    1. Легкое изменение значение в одном месте, что уменьшает вероятность опечатки значния
    2. Именнованная константа дает осмысленное имя значению, благодаря чему код легче читать и поддерживать.
    3. В случаи опечатки именнованной константы интерпретатор python об этом скажет
2.41 * DISCOUNT = 0.01
