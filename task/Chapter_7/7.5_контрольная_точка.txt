7.15: list.remove() применяется к объекту list, при этом обращение к удаляемому значение не происходит. Оператор del все
    лишь удаляет ссылку на объект, которая лежит в списке, обратившись по ней.

7.16: для минимума - min(list), для максимума - max(list)

7.17: б) names.append('Вэнди')

7.18:

    a) index(value) принимает значение, возвращает индекс значения в списке, иначе ValueError.

    б) insert(idx, value) вставляет значение value в список на позицию idx.

    в) sort() сортирует значения в списке по не убыванию, если значения можно сравнивать друг с другом.

    г) reverse() задаёт противоположный порядок элементов списка

